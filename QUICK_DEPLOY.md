# 🚀 Швидкий старт для деплою

## Що змінилось для деплою на сервер

### 🔧 Налаштування
- **Порт**: Змінено з 8000 на **8765** (щоб уникнути конфліктів)
- **Host**: Тепер `0.0.0.0` (слухає всі мережеві інтерфейси)
- **CORS**: Дозволено підключення з будь-яких IP адрес

### 📦 Файли для деплою

1. **`deploy-server.sh`** - Автоматичний деплой через Docker
2. **`run-local-8765.sh`** - Локальне тестування на порту 8765
3. **`DEPLOY.md`** - Повна інструкція з деплою
4. **`docker-compose.yml`** - Оновлено для порту 8765
5. **`Dockerfile`** - Оновлено для порту 8765

### ⚡ Швидкий деплой

#### На сервері:

```bash
# 1. Завантажити проект на сервер
git clone <your-repo>
cd bunker-game

# 2. Запустити деплой
chmod +x deploy-server.sh
./deploy-server.sh

# 3. Готово! Гра доступна на http://YOUR_SERVER_IP:8765
```

#### Локальне тестування:

```bash
# Протестувати на порту 8765 перед деплоєм
./run-local-8765.sh

# Відкрити в браузері:
# http://localhost:8765
# або з іншого пристрою в мережі:
# http://YOUR_LOCAL_IP:8765
```

### 🌐 Доступ до гри

Після запуску на сервері, гравці можуть підключитись через:

```
http://YOUR_SERVER_IP:8765
```

Де `YOUR_SERVER_IP` - це IP адреса вашого сервера.

### 🔥 Firewall

Не забудьте відкрити порт 8765:

```bash
# Ubuntu/Debian
sudo ufw allow 8765/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=8765/tcp
sudo firewall-cmd --reload
```

### 📊 Корисні команди

```bash
# Переглянути логи
docker-compose logs -f

# Перезапустити
docker-compose restart

# Зупинити
docker-compose down

# Оновити після змін коду
docker-compose up -d --build
```

### 🔒 Безпека для production

Якщо деплоїш на production (не приватний сервер):

1. Змінити `SECRET_KEY` в `.env`
2. Додати SSL/HTTPS (certbot + nginx)
3. Налаштувати rate limiting
4. Використовувати PostgreSQL замість SQLite
5. Зробити backup бази даних

Детальні інструкції в [DEPLOY.md](DEPLOY.md)

### ❓ Troubleshooting

**Порт зайнятий?**
```bash
sudo lsof -i :8765
sudo kill -9 <PID>
```

**Docker не працює?**
```bash
sudo systemctl start docker
```

**База даних не створюється?**
```bash
mkdir -p data
chmod 755 data
```

---

## 🎯 Різниця між режимами

| Режим | Команда | Призначення | Доступ |
|-------|---------|-------------|--------|
| **ngrok** | `./run-ngrok.sh` | Тестування з друзями | Тимчасовий публічний URL |
| **Локальний тест** | `./run-local-8765.sh` | Перевірка перед деплоєм | Локальна мережа |
| **Server Docker** | `./deploy-server.sh` | Production деплой | Постійний IP сервера |

---

Докладніше читай в [DEPLOY.md](DEPLOY.md)
