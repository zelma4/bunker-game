# Оновлення Гри Бункер - Інструкції для Запуску

## Що було виправлено

1. ✅ **Реал-тайм оновлення**: Інформація про людей та події тепер оновлюється автоматично через WebSocket без перезавантаження сторінки
2. ✅ **Відкриті картки гравців**: Тепер видно не тільки тип картки, а й її вміст (професія, біологія тощо)
3. ✅ **Таймер 1 хвилина**: Кожен гравець має 60 секунд на раунд відкриття карток
4. ✅ **Мобільна адаптація**: Повністю responsive дизайн з боковими меню для мобільних пристроїв
5. ✅ **Favicon**: Додано іконку сайту (більше немає помилки 404)
6. ✅ **Відповідність promt.md**: Всі правила, таймери та фази перевірені

## Як запустити

### Варіант 1: З ngrok для тестування з друзями 🌐 (РЕКОМЕНДОВАНО)

**Для чого потрібно**: ngrok створює публічний URL, який ви можете поділитися з друзями для спільної гри через інтернет.

```bash
./run-ngrok.sh
```

**Що робить скрипт:**
1. Автоматично запускає FastAPI сервер
2. Створює ngrok тунель (публічне посилання)
3. Показує URL який можна поділитися з друзями

**Важливо**: 
- Скопіюйте ngrok URL (наприклад, `https://abc123.ngrok.app`) 
- Поділіться цим посиланням з друзями
- Всі зможуть грати разом через інтернет!
- Для зупинки натисніть `Ctrl+C`

### Варіант 2: Через Docker
```bash
docker-compose up --build
```

### Варіант 3: Локально з віртуальним середовищем

1. **Активуйте віртуальне середовище** (якщо ще не активоване):
```bash
source venv/bin/activate
```

2. **Встановіть залежності** (якщо ще не встановлені):
```bash
cd backend
pip install -r requirements.txt
```

3. **Запустіть сервер**:
```bash
# З кореневої директорії проекту
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

4. **Відкрийте в браузері**:
```
http://localhost:8000
```

### Варіант 4: Через скрипти

**Mac/Linux:**
```bash
./run.sh
```

**Windows:**
```bash
run.bat
```

## Тестування змін

### 1. Перевірка реал-тайм оновлення
- Відкрийте гру в двох різних вікнах браузера
- Створіть гру в одному вікні
- Приєднайтесь до гри в іншому вікні
- Почніть гру та відкрийте картку в одному вікні
- Перевірте, що в іншому вікні картка відображається автоматично

### 2. Перевірка відображення карток
- Коли гравець відкриває картку, інші гравці повинні бачити:
  - Іконку типу картки (🎭, 🧬, 💊 тощо)
  - Повний текст картки (наприклад, "Лікар", "Чоловік, 35 років")

### 3. Перевірка таймера
- На фазі "Відкривання Карток" таймер повинен показувати 1 хвилину помножену на кількість гравців
- Наприклад, для 4 гравців: 4 хвилини (240 секунд)

### 4. Перевірка мобільної версії
- Відкрийте гру на мобільному пристрої або в режимі розробника (F12 → Device Toolbar)
- Повинні з'явитися дві круглі кнопки внизу екрану:
  - Ліва кнопка (☰) - відкриває меню "Мої Картки"
  - Права кнопка (💬) - відкриває чат
- Натисніть кнопки та перевірте, що сайдбари відкриваються/закриваються

### 5. Перевірка favicon
- Подивіться на вкладку браузера - повинна бути іконка бункера з радіацією

## Структура змін

```
backend/
├── app/
│   ├── routers/
│   │   └── games.py                    # ✏️ Додано WebSocket broadcasting
│   ├── services/
│   │   └── game_service.py             # Без змін
│   ├── websockets/
│   │   └── connection_manager.py       # ✏️ Оновлено метод send_player_revealed_card
│   └── game_logic.py                   # ✏️ Змінено таймер card_reveal на 60 сек

frontend/
├── static/
│   ├── css/
│   │   └── main.css                    # ✏️ Додано мобільну адаптацію
│   ├── js/
│   │   └── game.js                     # ✏️ Оновлено обробку WebSocket повідомлень
│   └── favicon.svg                     # ✨ НОВИЙ ФАЙЛ
├── templates/
│   ├── game.html                       # ✏️ Додано мобільні кнопки меню
│   └── index.html                      # ✏️ Додано favicon

CHANGES.md                               # ✨ НОВИЙ ФАЙЛ - детальний список змін
```

## Відомі обмеження

- 304 Not Modified для статичних файлів - це нормально (кешування браузера)
- Деякі linting warnings в Python коді (довгі рядки, trailing whitespace) - не впливають на роботу

## Підтримка

Якщо виникли проблеми:
1. Перевірте, чи запущений backend сервер
2. Перевірте консоль браузера (F12) на помилки
3. Перевірте логи FastAPI в терміналі
4. Переконайтесь, що порт 8000 не зайнятий іншим процесом

## Наступні покращення (опціонально)

- Анімації flip для карток (CSS 3D transform)
- Система Особливих Умов з ефектами
- Повна реалізація режиму "Історія Виживання"
- Покращення UX мобільної версії
- Додавання звукових ефектів
