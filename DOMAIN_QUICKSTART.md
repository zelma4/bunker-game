# ⚡ Швидка шпаргалка: bunker.zelma4.me

## 📋 Чек-лист налаштування

### 1️⃣ Namecheap (5 хвилин)
- [ ] Зайти на namecheap.com → Domain List → zelma4.me → Manage
- [ ] Advanced DNS → Add New Record
- [ ] Тип: **A Record**
- [ ] Host: **bunker**
- [ ] Value: **IP_ТВОГО_СЕРВЕРА**
- [ ] TTL: **Automatic**
- [ ] Зберегти ✓

### 2️⃣ На сервері (10 хвилин)

```bash
# Завантажити проект
git clone https://github.com/zelma4/bunker-game
cd bunker-game

# Автоматичне налаштування домену + SSL
sudo ./setup-domain.sh

# Запустити гру
./deploy-server.sh
```

### 3️⃣ Готово! 🎉
Відкрий: **https://bunker.zelma4.me**

---

## 🔧 Якщо щось не працює

### DNS не оновився
```bash
# Перевірка DNS (зачекай 5-30 хв)
dig bunker.zelma4.me
nslookup bunker.zelma4.me
```

### SSL помилка
```bash
# Спробувати ще раз
sudo certbot --nginx -d bunker.zelma4.me

# Перевірити логи
sudo tail -f /var/log/nginx/bunker.zelma4.me.error.log
```

### Додаток не працює
```bash
# Перевірити чи працює на порту 8765
curl http://localhost:8765

# Переглянути логи Docker
docker-compose logs -f

# Перезапустити все
docker-compose restart
sudo systemctl restart nginx
```

---

## 📝 Важливі файли

| Файл | Призначення |
|------|-------------|
| `DOMAIN_SETUP.md` | Повна інструкція з налаштування домену |
| `setup-domain.sh` | Автоматичний скрипт налаштування |
| `nginx-domain.conf` | Конфігурація Nginx |
| `deploy-server.sh` | Запуск Docker додатку |

---

## 🌐 Після налаштування

Гра буде доступна на:
- **https://bunker.zelma4.me** ← Основне посилання
- Автоматичний redirect з HTTP на HTTPS
- SSL сертифікат від Let's Encrypt (безкоштовно)
- WebSocket працює через wss://

**Поділись посиланням з друзями!** 🎮
