# 🔍 Проблема: Немає підключення після deploy

## Причина

`deploy-server.sh` **тільки запускає Docker**, але:
- Docker слухає на `localhost:8765` (не доступний ззовні)
- Немає Nginx для проксі з інтернету
- Порт 8765 може бути закритий файрволом

## 📊 Архітектура

### ❌ Що є зараз після deploy:
```
Internet → ⛔ заблоковано → localhost:8765 (Docker)
```

### ✅ Що має бути:
```
Internet → Nginx :80 → localhost:8765 (Docker)
```

## 🚀 Рішення

### Варіант 1: З Nginx (РЕКОМЕНДОВАНО)

```bash
# 1. Запустити Docker
./deploy-server.sh

# 2. Налаштувати Nginx (буде питати про домен)
sudo ./setup-nginx.sh

# Тепер доступ через:
# http://YOUR_SERVER_IP (без порту!)
```

### Варіант 2: Прямий доступ до порту 8765

```bash
# 1. Запустити Docker
./deploy-server.sh

# 2. Відкрити порт в файрволі
./fix-firewall.sh

# Тепер доступ через:
# http://YOUR_SERVER_IP:8765 (з портом)
```

### Варіант 3: З доменом та SSL

```bash
# 1. Налаштувати DNS на Namecheap (A record)
# 2. Запустити автоматичний скрипт
sudo ./setup-domain.sh

# Це зробить все автоматично:
# - Встановить Nginx
# - Налаштує для домену
# - Отримає SSL сертифікат
# - Запустить Docker

# Доступ через:
# https://bunker.zelma4.me
```

## 📋 Оновлений deploy-server.sh

Скрипт тепер **автоматично перевіряє** чи є Nginx після запуску Docker і пропонує його налаштувати.

```bash
./deploy-server.sh

# Якщо Nginx не налаштований, побачиш:
# ⚠️ Nginx not installed - port 8765 will only be accessible if firewall allows it
# Options:
#   1. Open port 8765: ./fix-firewall.sh
#   2. Install Nginx: ./setup-nginx.sh
#   3. Setup with domain: ./setup-domain.sh
```

## 🎯 Рекомендований порядок дій

### Без домену (швидкий старт):
```bash
./deploy-server.sh       # Запустить Docker
sudo ./setup-nginx.sh    # Налаштує Nginx (відповідь: n - немає домену)
# Готово! → http://YOUR_SERVER_IP
```

### З доменом bunker.zelma4.me:
```bash
# 1. На Namecheap: додати A record (bunker → IP сервера)
# 2. На сервері:
sudo ./setup-domain.sh   # Зробить все автоматично
# Готово! → https://bunker.zelma4.me
```

## 🔧 Чому Nginx краще ніж прямий порт?

| Функція | Порт 8765 | Nginx |
|---------|-----------|-------|
| **Стандартний порт** | ❌ :8765 | ✅ :80 |
| **HTTPS/SSL** | ❌ | ✅ |
| **Кешування** | ❌ | ✅ |
| **Rate limiting** | ❌ | ✅ |
| **Security headers** | ❌ | ✅ |
| **Логи** | Docker | Nginx + Docker |
| **Домен** | Складно | ✅ |

## 💡 Підсумок

**Проблема була**: Docker працював, але без Nginx немає доступу ззовні.

**Рішення**: Після `deploy-server.sh` обов'язково запустити `setup-nginx.sh` або `setup-domain.sh`.

**Оновлено**: `deploy-server.sh` тепер сам підказує що робити далі!
