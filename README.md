# 🎮 Гра Бункер (Bunker Game)

**Повнофункціональна онлайн дискусійна гра з постапокаліптичним сюжетом для 4-16 гравців**

![Version](https://img.shields.io/badge/version-2.0.0-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.115+-green)
![Python](https://img.shields.io/badge/Python-3.12+-yellow)
![License](https://img.shields.io/badge/license-MIT-purple)

## 🚀 Швидкий старт з друзями

**Щоб грати з друзями через інтернет:**

```bash
./run-ngrok.sh
```

Скопіюйте ngrok URL (наприклад, `https://abc123.ngrok.app`) та поділіться з друзями!

**Або деплой на власний сервер з доменом:**

```bash
# Повна інструкція: DOMAIN_SETUP.md
sudo ./setup-domain.sh  # Автоматично налаштує Nginx + SSL
./deploy-server.sh      # Запустить гру
```

Доступ: `https://bunker.your-domain.com` 🔒

**Або деплой на VPS без домену:**

```bash
# Детальна інструкція: DEPLOY.md
./deploy-server.sh
```

Доступ: `http://YOUR_SERVER_IP:8765`

**З власним доменом (наприклад, bunker.zelma4.me):**

```bash
# На сервері після деплою
./setup-nginx.sh
sudo certbot --nginx -d bunker.zelma4.me
```

Детальніше: [DOMAIN_SETUP.md](DOMAIN_SETUP.md)

---

## 📖 Огляд

**Гра бункер** - це соціальна гра виживання, де гравці намагаються переконати інших, що саме вони заслуговують місце в рятівному бункері під час глобальної катастрофи. Тільки половина гравців зможе вижити!

### 🎯 Як грати

1. **Створити кімнату** - Хост вибирає режим та ціль гри
2. **Приєднатися** - Гравці використовують код кімнати (4-16 осіб)
3. **Отримати картки** - Кожен гравець отримує 6 карток персонажа + особливу умову
4. **5 раундів** - По черзі відкривайте картки, аргументуйте, голосуйте
5. **Виживіть** - Той, хто залишився в бункері, виграв!

## ✨ Повні можливості (згідно офіційних правил)

### 📋 Картки персонажа (6 типів)
- 🎭 **Професія** (50 варіантів) - Лікар, Інженер, Військовий...
- 🧬 **Біологія** (32 варіанти) - Вік від 10 до 101 року, стать
- 💊 **Здоров'я** (32 варіанти) - Від спортивної форми до хронічних хвороб
- 🎨 **Хобі** (32 варіанти) - Садівництво, Програмування, Риболовля...
- 🎒 **Багаж** (32 варіанти) - Аптечка, Насіння, Дрон Лелека-100...
- 📄 **Факти** (50 варіантів) - Цікаві деталі про персонажа

### 🃏 Спеціальні картки
- **32 Особливі Умови** - Унікальні здібності (Шпигун, Лідер, Диверсант...)
- **10 Катастроф** - Двосторонні картки (Ядерна війна, Зомбі, Інопланетяни...)
- **30 Карток Бункера** - Опис обладнання бункера
- **21 Картка Загроз** - Для режиму "Історія виживання"

### 🎮 Режими гри
1. **Базовий** - Швидкий фінал після 5 раундів
2. **Історія виживання** - З перевіркою виживання та загрозами

### 🎯 Цілі
1. **Порятунок** (до 7 гравців) - Просто потрапити в бункер
2. **Відродження** (8+ гравців) - Потрібна пара різної статі

## 🚀 Швидкий старт

### Вимоги
- Python 3.12+
- pip
- ngrok (для гри з друзями через інтернет)

### Встановлення ngrok

```bash
# macOS
brew install ngrok

# Linux
curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
sudo apt update && sudo apt install ngrok

# Windows
# Завантажте з https://ngrok.com/download
```

### Швидкий запуск з ngrok (РЕКОМЕНДОВАНО для гри з друзями)

```bash
# 1. Запустіть скрипт
./run-ngrok.sh

# 2. Скопіюйте ngrok URL з терміналу (наприклад, https://abc123.ngrok.app)
# 3. Поділіться цим URL з друзями
# 4. Всі можуть грати разом через інтернет!
```

### Локальне встановлення

```bash
# 1. Клонувати репозиторій
git clone https://github.com/your-repo/bunker-game
cd bunker-game

# 2. Створити віртуальне середовище
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. Встановити залежності
pip install -r backend/requirements.txt

# 4. Запустити сервер
uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
```

Відкрийте браузер: **http://localhost:8000**

### Docker

```bash
docker-compose up --build
```

### 📝 Примітка про ngrok

- 🌐 ngrok створює публічний URL для доступу до вашого локального сервера
- 🆓 Безкоштовний план: URL змінюється при кожному запуску, 40 req/min
- 💎 Платний план: постійний URL та більше можливостей
- ⚡ Ідеально для швидкого тестування з друзями!

## 📁 Структура проекту

```
bunker-game/
├── backend/
│   ├── app/
│   │   ├── card_data.py         # 321+ картка
│   │   ├── game_logic.py        # Логіка раундів
│   │   ├── models.py            # Моделі БД (6 карток, режими)
│   │   ├── schemas.py           # Pydantic валідація
│   │   ├── services/
│   │   │   └── game_service.py  # Бізнес-логіка
│   │   ├── routers/
│   │   │   ├── games.py         # API гри
│   │   │   └── chat.py          # Чат
│   │   └── websockets/          # Real-time
│   └── requirements.txt
├── frontend/
│   ├── static/
│   │   ├── css/main.css
│   │   └── js/
│   │       ├── landing.js
│   │       └── game.js
│   └── templates/
│       ├── index.html
│       └── game.html
├── docker-compose.yml
└── README.md
```

## 🎲 Правила гри

### Підготовка
1. Хост створює кімнату (вибирає режим/ціль)
2. 4-16 гравців приєднуються за кодом
3. Хост запускає гру → автоматична роздача карток

### 5 Раундів

#### Раунд 1-5:
1. **Відкривання картки бункера** (10 сек) - Один гравець відкриває
2. **Відкривання карток персонажа** (60 сек/гравець) - По черзі
   - 📌 Раунд 1: Всі **обов'язково** відкривають Професію
3. **Обговорення** (1 хв) - Вільна дискусія
4. **Голосування** (1 хв) - За кого проголосувати (тільки з раунду 2!)
5. **Результат** (10 сек) - Вигнання гравця з найбільшою кількістю голосів

### Таблиця голосувань (кількість вигнань за раунд)

| Гравців | Р1 | Р2 | Р3 | Р4 | Р5 | Всього |
|---------|----|----|----|----|----|----|
| 4-7     | 0  | 1  | 1  | 1  | 1  | 4   |
| 8       | 0  | 1  | 1  | 2  | 1  | 5   |
| 9       | 0  | 1  | 2  | 2  | 1  | 6   |
| 16      | 0  | 3  | 3  | 4  | 3  | 13  |

> **Примітка:** Раунд 1 завжди без голосування - гравці спочатку знайомляться та відкривають професії.

---

## 🔄 Детальна логіка роботи гри

### Фази гри (GamePhase)

```
LOBBY → BUNKER_REVEAL → CARD_REVEAL → DISCUSSION → VOTING → REVEAL → (наступний раунд або ENDED)
```

| Фаза | Опис | Тривалість |
|------|------|------------|
| `lobby` | Очікування гравців | Необмежено |
| `bunker_reveal` | Показ картки бункера | 10 сек |
| `card_reveal` | Відкривання карток гравців | 60 сек × кількість гравців |
| `discussion` | Обговорення | 60 сек |
| `voting` | Голосування за вигнання | 60 сек |
| `reveal` | Показ результатів голосування | 10 сек |
| `survival_check` | Перевірка виживання (тільки режим "Історія виживання") | 30 сек |
| `ended` | Гра завершена | - |

### Процес переходу фаз

```
1. Гра починається (start_game)
   └── phase: LOBBY → BUNKER_REVEAL
   └── Роздача карток всім гравцям
   └── Генерація катастрофи та 5 карток бункера

2. BUNKER_REVEAL (10 сек)
   └── Відкривається 1 картка бункера
   └── Автоматичний перехід → CARD_REVEAL

3. CARD_REVEAL (60 сек × кількість гравців)
   └── Гравці по черзі відкривають свої картки
   └── В раунді 1 обов'язково відкривають професію
   └── Автоматичний перехід → DISCUSSION

4. DISCUSSION (60 сек)
   └── Вільне обговорення в чаті
   └── Перевірка: чи є голосування в цьому раунді?
   └── Якщо раунд 1 (0 голосувань) → пропускаємо VOTING
   └── Автоматичний перехід → VOTING або BUNKER_REVEAL (наступний раунд)

5. VOTING (60 сек) - тільки якщо є голосування
   └── Кожен гравець голосує за 1 кандидата на вигнання
   └── Не можна голосувати за себе
   └── WebSocket транслює оновлення голосів всім
   └── Автоматичний перехід → REVEAL

6. REVEAL (10 сек)
   └── Визначення переможця (найбільше голосів)
   └── При нічиї - випадковий вибір серед лідерів
   └── Вигнаний гравець отримує статус ELIMINATED
   └── Всі його картки (крім особливої умови) відкриваються
   └── WebSocket транслює player_eliminated всім
   └── Скидання голосів для наступного раунду
   └── Перевірка: чи потрібен наступний раунд?

7. Наступний раунд або кінець гри
   └── Якщо alive_count <= bunker_capacity АБО round >= 5
       └── Базовий режим → phase: ENDED
       └── Історія виживання → phase: SURVIVAL_CHECK
   └── Інакше → current_round++, phase: BUNKER_REVEAL
```

### Визначення місткості бункера

```python
# Формула: кількість_гравців - загальна_кількість_вигнань
bunker_capacity = player_count - sum(votings_per_round)

# Приклади:
# 4 гравці: 4 - 4 = 0 місць (всі мають бути вигнані? Ні, перевірка >= йде раніше)
# Реально: місткість = player_count // 2 (половина виживає)
```

### WebSocket події

| Подія | Коли відправляється | Дані |
|-------|---------------------|------|
| `game_update` | Оновлення стану гри | phase, current_round, players |
| `phase_change` | Зміна фази | phase, phase_end_time |
| `player_joined` | Новий гравець | player_name |
| `player_left` | Гравець вийшов | player_name |
| `bunker_card_revealed` | Відкрито картку бункера | revealed_count |
| `player_revealed_card` | Гравець відкрив картку | player_id, card_type, card_value |
| `vote_update` | Оновлення голосів | {player_id: {votes_received, has_voted}} |
| `player_eliminated` | Гравця вигнано | player_id, player_name, revealed_cards |
| `special_card_used` | Використано особливу умову | player_name, special_name |
| `chat` | Повідомлення в чаті | player_name, message |

### Таймери та автоматичний перехід фаз

```javascript
// Frontend (game.js) відстежує таймер
setInterval(() => {
    const remaining = phase_end_time - now;
    
    if (remaining <= 0 && isHost) {
        // Хост автоматично викликає advance-phase
        fetch(`/api/games/${gameId}/advance-phase`, { method: 'POST' });
    }
    
    // Fallback: якщо хост не відповідає 3 секунди, будь-хто може
    if (remaining <= -3 && !isHost) {
        fetch(`/api/games/${gameId}/advance-phase`, { method: 'POST' });
    }
}, 1000);
```

### Захист від помилок

1. **Rate limiting advance-phase** - не частіше 1 раз/сек
2. **Захист від вигнання без голосів** - якщо max_votes = 0, ніхто не вигнаний
3. **Перевірка фази для голосування** - можна голосувати тільки в фазі VOTING
4. **Перевірка has_voted** - не можна голосувати двічі
5. **Скидання голосів** - при переході до нового раунду

---

### Фінал

**Базовий режим:**
- Хто залишився → відкривають всі картки
- Перевірка: чи досягнута ціль (Порятунок/Відродження)

**Історія виживання:**
1. Роздача карток Загроз (кожному в бункері)
2. Голосування: чи може група впоратись?
3. Ті ж загрози для вигнанців (без карток бункера)
4. Перевірка: хто вижив + чи досягнута мета

---

## 🔧 API Endpoints

### Гра
| Метод | Endpoint | Опис |
|-------|----------|------|
| `POST` | `/api/games/create` | Створити гру (mode, goal, player_name) |
| `POST` | `/api/games/join` | Приєднатись до гри (code, player_name) |
| `GET` | `/api/games/{code}` | Отримати інформацію про гру |
| `POST` | `/api/games/{id}/start` | Почати гру (тільки хост) |
| `POST` | `/api/games/{id}/reveal-card` | Відкрити свою картку (card_type) |
| `POST` | `/api/games/{id}/vote` | Проголосувати (target_player_id) |
| `POST` | `/api/games/{id}/advance-phase` | Перейти до наступної фази |
| `POST` | `/api/games/{id}/use-special` | Використати особливу умову |
| `GET` | `/api/games/{id}/my-character` | Отримати свої картки |

### Чат
| Метод | Endpoint | Опис |
|-------|----------|------|
| `POST` | `/api/chat/{game_id}/messages` | Надіслати повідомлення |
| `GET` | `/api/chat/{game_id}/messages` | Отримати історію чату |

### WebSocket
| Endpoint | Опис |
|----------|------|
| `WS /ws/{game_code}` | Real-time оновлення гри |

### Приклади запитів

**Створити гру:**
```bash
curl -X POST http://localhost:8000/api/games/create \
  -H "Content-Type: application/json" \
  -d '{"player_name": "Гравець1", "mode": "basic", "goal": "salvation"}'
```

**Приєднатись до гри:**
```bash
curl -X POST http://localhost:8000/api/games/join \
  -H "Content-Type: application/json" \
  -d '{"code": "ABC123", "player_name": "Гравець2"}'
```

**Проголосувати:**
```bash
curl -X POST http://localhost:8000/api/games/1/vote \
  -H "Content-Type: application/json" \
  -d '{"target_player_id": 3}'
```

---

## 🛠️ Технології

- **Backend:** FastAPI 0.115+, SQLAlchemy 2.0+, WebSockets
- **Frontend:** HTMX 2.0+, Alpine.js 3.15+, Tailwind CSS 3.4+
- **Database:** SQLite (PostgreSQL supported)
- **Deployment:** Docker, Docker Compose

## 🎨 Особливості реалізації

### Картки (321+)
- ✅ 50 професій
- ✅ 32 біології (вік + стать)
- ✅ 32 здоров'я
- ✅ 32 хобі
- ✅ 32 багажу
- ✅ 50 фактів
- ✅ 32 особливі умови
- ✅ 10 катастроф (двосторонні)
- ✅ 30 карток бункера
- ✅ 21 картка загроз

### Ігрові механіки
- ✅ Покрокове відкривання карток
- ✅ Таблиця раундів (4-16 гравців)
- ✅ Обов'язкова професія в раунді 1
- ✅ Режими: Базовий/Історія виживання
- ✅ Цілі: Порятунок (≤7) / Відродження (≥8)
- ✅ 6 фаз гри: BUNKER_REVEAL → CARD_REVEAL → DISCUSSION → VOTING → REVEAL → ENDED
- ✅ Таймери для кожної фази з автоматичним переходом
- ✅ Картки загроз для фіналу
- ✅ Real-time через WebSocket
- ✅ Rate limiting для API

### Система голосування
- ✅ Голосування тільки з раунду 2
- ✅ Не можна голосувати за себе
- ✅ Real-time оновлення голосів через WebSocket
- ✅ Автоматичне вигнання гравця з найбільшою кількістю голосів
- ✅ Випадковий вибір при нічиї
- ✅ Скидання голосів після кожного раунду

### Синхронізація та WebSocket
- ✅ Автоматичний перехід фаз по таймеру
- ✅ Хост ініціює перехід, fallback - будь-який гравець через 3 сек
- ✅ Трансляція всіх подій: голоси, відкриття карток, вигнання
- ✅ Обробка відключень та перепідключень

### Безпека
- ✅ Session ID через cookies
- ✅ Rate limiting на advance-phase (1 запит/сек)
- ✅ Валідація всіх дій на backend
- ✅ Перевірка фази гри для кожної дії

## 🎯 Наступні кроки

### В розробці
- 🔄 SVG картки з різнокольоровими рамками
- 🔄 Модальні вікна вибору режиму/цілі
- 🔄 Анімація перевертання карток
- 🔄 Фінальний екран з результатами
- 🔄 Використання особливих умов
- 🔄 QR-коди для катастроф (аудіо)

### Заплановано
- [ ] JWT auth
- [ ] Redis для сесій
- [ ] Rate limiting (SlowAPI)
- [ ] Експорт історії гри (JSON/PDF)
- [ ] Статистика професій
- [ ] Режим для 2-3 гравців
- [ ] Pytest покриття ≥80%
- [ ] GitHub Actions CI/CD

## 📝 Ліцензія

MIT License - дивіться [LICENSE](LICENSE)

## 🤝 Внесок

Вітаються pull requests! Для великих змін спочатку відкрийте issue.

## 📧 Контакти

- Issues: [GitHub Issues](https://github.com/your-repo/bunker-game/issues)
- Discussions: [GitHub Discussions](https://github.com/your-repo/bunker-game/discussions)

---

**Зроблено з ❤️ для виживання у постапокаліпсисі**

🎮 FastAPI • HTMX • Alpine.js • Tailwind CSS
