# 🌐 Архітектура з доменом bunker.zelma4.me

```
┌─────────────────────────────────────────────────────────────────┐
│                         КОРИСТУВАЧ                               │
│                    (Браузер на телефоні/ПК)                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTPS Request
                            │ https://bunker.zelma4.me
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NAMECHEAP DNS                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  A Record:                                               │  │
│  │  bunker.zelma4.me → 123.45.67.89                        │  │
│  │  TTL: 300 (5 хвилин)                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ Resolves to Server IP
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ТВІЙ СЕРВЕР (VPS)                             │
│                   IP: 123.45.67.89                               │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    NGINX (Reverse Proxy)                   │ │
│  │  Port 80 (HTTP) → redirect to 443                         │ │
│  │  Port 443 (HTTPS) ↓                                       │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │  SSL/TLS Certificate                             │    │ │
│  │  │  (Let's Encrypt via Certbot)                     │    │ │
│  │  │  - fullchain.pem                                 │    │ │
│  │  │  - privkey.pem                                   │    │ │
│  │  │  Auto-renew every 90 days                        │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                            │ │
│  │  WebSocket Upgrade Support ✓                              │ │
│  │  proxy_pass → http://127.0.0.1:8765                       │ │
│  └────────────────┬───────────────────────────────────────────┘ │
│                   │                                              │
│                   │ Internal proxy                               │
│                   ▼                                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              DOCKER CONTAINER                              │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │         FastAPI Application                      │    │ │
│  │  │         (uvicorn)                                │    │ │
│  │  │                                                  │    │ │
│  │  │  Port: 8765 (internal)                          │    │ │
│  │  │  Host: 0.0.0.0                                  │    │ │
│  │  │                                                  │    │ │
│  │  │  ┌─────────────────────────────────────┐        │    │ │
│  │  │  │  WebSocket Server                   │        │    │ │
│  │  │  │  - Real-time game updates           │        │    │ │
│  │  │  │  - Chat messages                    │        │    │ │
│  │  │  │  - Player actions                   │        │    │ │
│  │  │  └─────────────────────────────────────┘        │    │ │
│  │  │                                                  │    │ │
│  │  │  ┌─────────────────────────────────────┐        │    │ │
│  │  │  │  REST API                           │        │    │ │
│  │  │  │  - /api/games                       │        │    │ │
│  │  │  │  - /api/chat                        │        │    │ │
│  │  │  │  - /ws/{game_code}                  │        │    │ │
│  │  │  └─────────────────────────────────────┘        │    │ │
│  │  │                                                  │    │ │
│  │  │  Static Files: /frontend/static                 │    │ │
│  │  │  Templates: /frontend/templates                 │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │         SQLite Database                          │    │ │
│  │  │         /data/bunker_game.db                     │    │ │
│  │  │                                                  │    │ │
│  │  │  - Games table                                  │    │ │
│  │  │  - Players table                                │    │ │
│  │  │  - Messages table                               │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    UFW Firewall                            │ │
│  │  - Port 80/tcp   (HTTP)  ✓                                │ │
│  │  - Port 443/tcp  (HTTPS) ✓                                │ │
│  │  - Port 8765/tcp (App)   ✓                                │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Потік даних

### 1. HTTP Request Flow
```
Користувач
   │
   │ https://bunker.zelma4.me/
   ▼
Namecheap DNS
   │
   │ Resolves to 123.45.67.89
   ▼
Server:443 (NGINX)
   │
   │ SSL Termination
   │ proxy_pass
   ▼
127.0.0.1:8765 (FastAPI)
   │
   │ Render HTML
   ▼
Повертає index.html
```

### 2. WebSocket Connection Flow
```
Користувач
   │
   │ wss://bunker.zelma4.me/ws/ABCD
   ▼
NGINX
   │
   │ Upgrade: websocket
   │ Connection: upgrade
   ▼
FastAPI WebSocket Handler
   │
   ├─→ Broadcast to all players
   ├─→ Update game state
   └─→ Send real-time updates
```

### 3. Static Files Flow
```
Користувач
   │
   │ https://bunker.zelma4.me/static/css/main.css
   ▼
NGINX
   │
   │ proxy_pass
   ▼
FastAPI StaticFiles
   │
   │ /frontend/static/css/main.css
   ▼
Повертає CSS файл
```

## 🔐 Security Layers

```
┌──────────────────────────────────────┐
│  1. HTTPS (TLS 1.2/1.3)              │ ← Let's Encrypt SSL
│     - Encrypted connection           │
│     - Certificate validation         │
└──────────────────────────────────────┘
              │
┌──────────────────────────────────────┐
│  2. UFW Firewall                     │ ← Only allowed ports
│     - Port 80, 443, 8765             │
│     - Block all other                │
└──────────────────────────────────────┘
              │
┌──────────────────────────────────────┐
│  3. NGINX Proxy                      │ ← Hide internal structure
│     - Rate limiting                  │
│     - Request filtering              │
└──────────────────────────────────────┘
              │
┌──────────────────────────────────────┐
│  4. Docker Isolation                 │ ← Container security
│     - Isolated network               │
│     - Resource limits                │
└──────────────────────────────────────┘
              │
┌──────────────────────────────────────┐
│  5. FastAPI CORS                     │ ← Origin validation
│     - Allowed origins list           │
│     - Credential checking            │
└──────────────────────────────────────┘
```

## 📊 Port Mapping

```
External (Internet)         Internal (Server)
─────────────────────────────────────────────
Port 80  (HTTP)      →     NGINX
Port 443 (HTTPS)     →     NGINX
                            │
                            └─→  Port 8765 (FastAPI in Docker)
                                      │
                                      └─→ SQLite DB (filesystem)
```

## 🌍 Domain Resolution Steps

```
1. Користувач вводить: bunker.zelma4.me
                         │
2. Browser DNS Query     │
                         ▼
3. Root DNS Servers (.me registry)
                         │
4. Namecheap NS Servers  │
                         ▼
5. A Record Resolution
   bunker.zelma4.me = 123.45.67.89
                         │
6. Browser connects      ▼
   to 123.45.67.89:443
```

## 🔄 Auto-Renewal (SSL Certificate)

```
Every 12 hours:
┌─────────────────────────────────────┐
│  Certbot Timer (systemd)            │
│  /etc/systemd/system/               │
│  certbot.timer                      │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  Check certificate expiry           │
│  (90 days validity)                 │
└────────────┬────────────────────────┘
             │
             │ If < 30 days left
             ▼
┌─────────────────────────────────────┐
│  Request new certificate            │
│  from Let's Encrypt                 │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  Reload Nginx                       │
│  (no downtime)                      │
└─────────────────────────────────────┘
```

## 🎯 Complete URL Structure

```
https://bunker.zelma4.me/                    → Landing page
https://bunker.zelma4.me/game/ABCD           → Game page
https://bunker.zelma4.me/static/css/main.css → Static files
https://bunker.zelma4.me/api/games           → REST API
wss://bunker.zelma4.me/ws/ABCD               → WebSocket
```

## 💡 Benefits of this Setup

✅ **Professional domain** instead of IP address
✅ **HTTPS encryption** for security
✅ **WebSocket support** for real-time gameplay
✅ **Auto SSL renewal** - no manual intervention
✅ **Nginx caching** and optimization
✅ **Easy scaling** - can add load balancer later
✅ **Docker isolation** - easy updates and rollbacks
✅ **Firewall protection** - only needed ports open
