services:
  app:
    build: .
    ports:
      - "8765:8765"
    environment:
      - DATABASE_URL=sqlite:////data/bunker_game.db
      - DEBUG=false
    volumes:
      - ./data:/data
    restart: unless-stopped

  # Optional: Redis for sessions (uncomment if needed)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   restart: unless-stopped
